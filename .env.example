# Required for embeddings (cosine similarity, dedup, density calculation)
OPENAI_API_KEY=sk-your-openai-api-key

# The memory system uses OpenAI's text-embedding-3-small model for embeddings.
# LLM calls (R scoring, precis writing, plan rewriting, edge identification)
# are routed through whatever model you pass to createCognitiveMemory().
